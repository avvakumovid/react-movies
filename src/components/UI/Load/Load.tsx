import React from 'react';
import load from './load.svg'


const Load = () => {
    return (
        <div>
            <img width={200} alt='loader' src={load}/>
        </div>);
};

export default Load;